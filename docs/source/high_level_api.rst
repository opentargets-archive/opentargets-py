.. _high_level_api:


High Level API
=============
The high level API allows you to directly querying each public endpoint the REST API,
with simple methods calls, that takes care of most common use cases

TODO